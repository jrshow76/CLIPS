# =====================================================================
# Tulip+ Member Service — application.yml (Sprint 1-C)
# ---------------------------------------------------------------------
# 회원/회원증/동의 도메인 CRUD + 변경 이벤트 발행 (Outbox).
# =====================================================================

server:
  port: 8103
  forward-headers-strategy: framework
  error:
    include-message: always

spring:
  application:
    name: member-service
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false
  datasource:
    url: jdbc:postgresql://localhost:5432/tulip
    username: tulip
    password: tulip
    hikari:
      maximum-pool-size: 8
      minimum-idle: 1
      connection-timeout: 5000
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    default-schema: public
    table: flyway_history_member
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2s
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${tulip.iam.issuer-uri:http://localhost:8088/realms/tulip}
          jwk-set-uri: ${tulip.iam.jwks-uri:http://localhost:8088/realms/tulip/protocol/openid-connect/certs}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      properties:
        enable.idempotence: true

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    default-fetch-size: 100
    default-statement-timeout: 5
  type-handlers-package: com.tulip.common.data.handler
  type-aliases-package: com.tulip.member.domain

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when_authorized

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
  packagesToScan: com.tulip.member

tulip:
  iam:
    issuer-uri: http://localhost:8088/realms/tulip
    jwks-uri: http://localhost:8088/realms/tulip/protocol/openid-connect/certs
    expected-audiences:
      - admin-web
      - opac-web
      - member-service
      - account
  member:
    pii-passphrase: ${TULIP_MEMBER_PII_PASSPHRASE:dev-pii-passphrase-change-me}
    outbox:
      poll-interval-ms: 1000
      batch-size: 100
    soft-delete-grace-days: 30

logging:
  level:
    root: INFO
    com.tulip: DEBUG
    org.springframework.security: INFO
