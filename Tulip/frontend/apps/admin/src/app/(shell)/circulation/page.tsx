import { DomainPlaceholder } from '../_components/DomainPlaceholder';

export const metadata = { title: '열람 — Tulip+ Admin' };

export default function Page() {
  return <DomainPlaceholder domain="cir" />;
}
