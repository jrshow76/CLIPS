import { DomainPlaceholder } from '../_components/DomainPlaceholder';

export const metadata = { title: '목록 — Tulip+ Admin' };

export default function Page() {
  return <DomainPlaceholder domain="cat" />;
}
