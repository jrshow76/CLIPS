import { DomainPlaceholder } from '../_components/DomainPlaceholder';

export const metadata = { title: '수서 — Tulip+ Admin' };

export default function Page() {
  return <DomainPlaceholder domain="acq" />;
}
