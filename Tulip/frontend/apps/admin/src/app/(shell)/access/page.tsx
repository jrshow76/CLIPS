import { DomainPlaceholder } from '../_components/DomainPlaceholder';

export const metadata = { title: '출입 — Tulip+ Admin' };

export default function Page() {
  return <DomainPlaceholder domain="acs" />;
}
