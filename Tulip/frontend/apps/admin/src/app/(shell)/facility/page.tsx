import { DomainPlaceholder } from '../_components/DomainPlaceholder';

export const metadata = { title: '시설 — Tulip+ Admin' };

export default function Page() {
  return <DomainPlaceholder domain="fac" />;
}
